1. markers 테이블 생성 sql
title : 약속 제목
created_by : 생성한 유저의 이름
context : 약속 상세 내용
latitude : 위도
longitude : 경도
max_number : 최대 인원 수
type : 약속의 종류(택시, 도서관 등)
image : 이미지 경로

CREATE TABLE `markers` (
  `id` varchar(255) NOT NULL,
  `title` varchar(255) NOT NULL,
  `created_by` varchar(255) NOT NULL,
  `context` text NOT NULL,
  `latitude` double NOT NULL,
  `longitude` double NOT NULL,
  `max_number` int NOT NULL,
  `type` varchar(255) NOT NULL,
  `image` char(20) NOT NULL,
  PRIMARY KEY (`id`)
)

2. markers 테이블 생성한 다음에는 map.html에서 마커 생성하면 자동으로 insert sql을 실행하기 때문에 추가적인 데이터를 미리 테이블에 저장하고 싶다면 사용할 insert sql
INSERT INTO markers (title, created_by, context, latitude, longitude, max_number, type) values 
('1', 'tp', 'gkgkg', 35.1, 129.5, 5, '택시');
